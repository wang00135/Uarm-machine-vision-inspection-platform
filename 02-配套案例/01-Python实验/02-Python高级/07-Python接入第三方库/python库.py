'''
请注意，每一个包目录下面都会有一个__init__.py的文件，这个文件是必须存在的，
否则，Python就把这个目录当成普通目录(文件夹)，
而不是一个包。init.py可以是空文件，也可以有Python代码，
因为__init__.py本身就是一个模块，而它的模块名就是对应包的名字。

'''
print('请注意，每一个包目录下面都会有一个__init__.py的文件，这个文件是必须存在的，'+'\n'
      '否则，Python就把这个目录当成普通目录(文件夹)，'+'\n'
      '而不是一个包。init.py可以是空文件，也可以有Python代码，'+'\n'
      '因为__init__.py本身就是一个模块，而它的模块名就是对应包的名字。')